import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  // colores
  // fuentes
  // espacios
  // sombres
  palette: {
    primary: {
      light: "#FB9204",
      main: "#E8C309",
      dark: "#E86009",
    },
    secondary: {
      light: "#0EBAE8",
      main: "#0E9FDF",
      dark: "#0ED9DF",
    },
  },
});

export default theme;
